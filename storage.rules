rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Storage is not used by the app; deny everything unless explicitly enabled.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
